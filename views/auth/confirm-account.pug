extends ../layout/index.pug

block content
  div(class="py-10 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8")
    h1(class="text-4xl my-10 font-extrabold text-center")
      span(class="bg-gradient-to-r from-gray-900 to-gray-700 bg-clip-text text-transparent") Real
      span(class="font-normal text-black") state 
    h2(class="text-center text-2xl md:text-3xl font-bold text-gray-800 mb-8")= page

    div(class="bg-white shadow-lg py-8 px-6 rounded-2xl max-w-md mx-auto")
      div(class=`${error ? 'bg-red-50 border-red-600' : 'bg-green-50 border-green-600'} border-l-4 p-4 rounded-lg mb-5`)
        p(class=`${error ? 'text-red-600' : 'text-green-600'} font-medium`)= message 

      if !error
        a(href="/auth/login" class="block text-center bg-black hover:bg-gray-800 text-white font-bold py-3 px-4 rounded-lg transition-colors mt-6") Iniciar sesión
