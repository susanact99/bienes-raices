extends ../layout/index.pug

block content

  div.py-10
    h1.text-4xl.my-10.font-extrabold.text-center Real
      span.font-normal state 
    h2.text-center.text-2xl.font-extrabold= page

  div(class="bg-white shadow py-8 px-4 rounded-lg max-w-md mx-auto")
    p(class=error ? 'text-red-500' : 'text-green-500').py-2.px-5.rounded-lg.w-full.font-bold.text-center.bg-white.uppercase= message 

    if !error
       a(href="/auth/login" class="text-center font-bold text-sm text-slate-600 uppercase mt-10 block") Now you can Login 